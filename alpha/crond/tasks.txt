su root

echo "" >> /etc/crontab
echo "# Kaltura's daily tasks" >> /etc/crontab
echo "15 5 * * * root run-parts /etc/cron.kaltura.daily" >> /etc/crontab
echo "" >> /etc/crontab

mkdir /etc/cron.kaltura.daily
chmod --reference=/etc/cron.daily /etc/cron.kaltura.daily

mkdir /opt/kaltura/logs/

# limelight tasks
ln -s /web/alpha/crond/limelight/ll_fetch_and_insert.sh ll_fetch_and_insert

# kaltura tasks
ln -s /web/alpha/crond/kaltura/delete_old_content.sh delete_old_content
ln -s /web/alpha/crond/kaltura/storage_update.sh storage_update
ln -s /web/alpha/crond/kaltura/www_logs_insert.sh www_logs_insert


service crond restart