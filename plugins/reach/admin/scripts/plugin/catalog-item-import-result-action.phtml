
<?php if($this->bulkUploadResult): ?>
<var> <?php foreach (get_object_vars($this->bulkUploadResult) as $key => $val)
        {
            echo  '<b>' . $key . ':</b>' .' ' . $val . '<br>';
		}
		echo "<br>"; ?>
</var>
<?php endif; ?>

<?php if($this->logFileUrl): ?>
    <var> <?php
		echo '<b> logFileUrl: </b> <br>';
		$ch = curl_init();
		curl_setopt($ch, CURLOPT_URL, $this->logFileUrl);
		$response =  curl_exec ($ch);
		curl_close ($ch);  ?>
    </var>
<?php endif; ?>

<?php if($this->errMessage): ?>
    <p>Error occurred: <var> <?php echo $this->errMessage; ?> </var> </p>
<?php endif; ?>

<style type="text/css">
	#catalog-item-import-result-dialog { width:auto ; left: auto; word-wrap: break-word}
</style>
